// Re-export the new drawing manager with the old name for backward compatibility
export { useDrawingManager as useDrawingService } from './drawing';
export type { InProgressDrawing } from './drawing';